############################################################################
# Contest 2026 team 345 ESP32-P4X Function EV Board linker configuration
############################################################################

include $(TOPDIR)/.config
include $(TOPDIR)/tools/Config.mk
include $(TOPDIR)/tools/espressif/Config.mk
include $(TOPDIR)/arch/risc-v/src/common/Toolchain.defs

# Explicit experimental build only: the upstream HWLP relocations need the
# pinned Espressif linker. The remaining compilation stays on project GCC13.
ifneq ($(VELAFIT_ESP_NN_LIB),)
LD = $(dir $(VELAFIT_PIE_CC))riscv32-esp-elf-ld
EXTRA_LIBS += $(VELAFIT_ESP_NN_LIB)
endif

CHIP_SERIES = $(patsubst "%",%,$(CONFIG_ESPRESSIF_CHIP_SERIES))

ARCHSCRIPT += $(BOARD_DIR)/scripts/$(CHIP_SERIES)_aliases.ld
ARCHSCRIPT += $(BOARD_DIR)/scripts/$(CHIP_SERIES)_flat_memory.ld
ARCHSCRIPT += $(BOARD_DIR)/scripts/$(CHIP_SERIES)_sections.rev3.ld

ARCHPICFLAGS = -fpic

CFLAGS := $(ARCHCFLAGS) $(ARCHOPTIMIZATION) $(ARCHCPUFLAGS) $(ARCHINCLUDES) $(ARCHDEFINES) $(EXTRAFLAGS) -Werror=return-type
CPICFLAGS = $(ARCHPICFLAGS) $(CFLAGS)
CXXFLAGS := $(ARCHCXXFLAGS) $(ARCHOPTIMIZATION) $(ARCHCPUFLAGS) $(ARCHXXINCLUDES) $(ARCHDEFINES) $(EXTRAFLAGS)
CXXPICFLAGS = $(ARCHPICFLAGS) $(CXXFLAGS)
CPPFLAGS := $(ARCHINCLUDES) $(ARCHDEFINES) $(EXTRAFLAGS)
AFLAGS := $(CFLAGS) -D__ASSEMBLY__

LDMODULEFLAGS += -melf32lriscv
LDELFFLAGS += -melf32lriscv
